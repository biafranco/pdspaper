#include "database.h"
#include <fstream>

using namespace std;

Database::Database(string path){}

string Database::normalize(string input) {}

void Database::pesquisa(vector<string> palavras){}

Database::~Database(){
}